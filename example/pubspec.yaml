name: multicamera_example
version: 1.0.0
publish_to: 'none'

environment:
  flutter: "3.47.4"
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  permission_handler: ^13.0.0
  multicamera:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
