name: multicamera_example
version: 1.0.0
publish_to: 'none'

environment:
  flutter: "3.44.2"
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  permission_handler: ^12.0.3
  multicamera:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
